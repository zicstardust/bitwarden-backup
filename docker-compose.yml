services:
  bitwarden-backup:
    container_name: bitwarden-backup
    image: bitwarden-backup:latest
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    volumes:
      - ./data/:/data/